#include <iostream>
using namespace std;

int main(){
float a, b, c;
float areatriangolo = ((a * b) / 2), areaquadrato = (a * a), arearettangolo = (a * b), areatrapezio = (((a + b) * c) / 2);
std::cin >> a >> b >> c;
std::cout << ((a * b) / 2) << std::endl;
std::cout << (a * a) << std::endl;
std::cout << (a * b) << std::endl;
std::cout << (((a + b) * c) / 2) << std::endl;
}
